/**
 * @module @thinkxx/config
 *
 * Shared configuration, constants, and types for the Thinkxx protocol.
 */

export { NETWORK_CONFIG, CLUSTER } from './network';
export { PLAN_DEFAULTS } from './defaults';
export { PROGRAM_ID, SEEDS } from './program';
export type {
  PlanMode,
  PlanState,
  ClaimState,
  TimingConfig,
  NetworkConfig,
} from './types';
