{
  "name": "@thinkxx/config",
  "version": "0.1.0",
  "private": true,
  "description": "Shared configuration, constants, and types for Thinkxx",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "lint": "eslint src/",
    "typecheck": "tsc --noEmit",
    "clean": "rm -rf dist"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "typescript": "^5.5.0",
    "eslint": "^9.0.0"
  }
}
